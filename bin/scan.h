#ifndef _SCAN_H
#define _SCAN_H

#include <stdio.h>
#include <wchar.h>

/* Scan the regular expression file
 * get rid of the space,\t and \n,
 * and create the array of char* for 
 * each line
 */

void Scan( char* );

#endif
